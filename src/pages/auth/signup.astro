---
import Layout from '../../layouts/Layout.astro'
---
<Layout title="Create Account — GAUK Antiques">
  <main class="min-h-screen bg-neutral-950 text-neutral-100 flex items-center justify-center p-8">
    <div class="w-full max-w-md">
      <div class="mb-8">
        <h1 class="text-lg font-semibold tracking-wide">GAUK <span class="text-amber-500">Antiques</span></h1>
      </div>
      <h2 class="text-2xl font-bold mb-2">Create account</h2>
      <p class="text-neutral-400 text-sm mb-8">Save your identifications and build your collection history.</p>

      <div id="error-msg" class="hidden mb-4 p-3 bg-red-950 border border-red-800 rounded-lg text-red-400 text-sm"></div>
      <div id="success-msg" class="hidden mb-4 p-3 bg-green-950 border border-green-800 rounded-lg text-green-400 text-sm"></div>

      <div class="space-y-4">
        <div>
          <label class="block text-sm text-neutral-400 mb-1">Email</label>
          <input id="email" type="email" class="w-full bg-neutral-900 border border-neutral-700 rounded-lg px-4 py-3 text-neutral-100 focus:outline-none focus:border-amber-500 transition-colors" placeholder="you@example.com" />
        </div>
        <div>
          <label class="block text-sm text-neutral-400 mb-1">Password</label>
          <input id="password" type="password" class="w-full bg-neutral-900 border border-neutral-700 rounded-lg px-4 py-3 text-neutral-100 focus:outline-none focus:border-amber-500 transition-colors" placeholder="Min 6 characters" />
        </div>
        <button id="signup-btn" class="w-full py-3 bg-amber-600 hover:bg-amber-500 rounded-xl font-semibold transition-colors">
          Create Account
        </button>
      </div>

      <p class="mt-6 text-center text-sm text-neutral-400">
        Already have an account? <a href="/auth/signin" class="text-amber-500 hover:text-amber-400">Sign in</a>
      </p>
    </div>
  </main>
</Layout>

<script>
  import { createClient } from '@supabase/supabase-js'

  const supabase = createClient(
    import.meta.env.PUBLIC_SUPABASE_URL,
    import.meta.env.PUBLIC_SUPABASE_ANON_KEY
  )

  const btn = document.getElementById('signup-btn')
  const errorMsg = document.getElementById('error-msg')
  const successMsg = document.getElementById('success-msg')

  btn?.addEventListener('click', async () => {
    const email = (document.getElementById('email') as HTMLInputElement).value
    const password = (document.getElementById('password') as HTMLInputElement).value

    if (!email || !password) {
      errorMsg!.textContent = 'Please fill in all fields'
      errorMsg!.classList.remove('hidden')
      return
    }

    btn.setAttribute('disabled', 'true')
    btn.textContent = 'Creating account...'
    errorMsg!.classList.add('hidden')

    const { error } = await supabase.auth.signUp({ email, password })

    if (error) {
      errorMsg!.textContent = error.message
      errorMsg!.classList.remove('hidden')
      btn.removeAttribute('disabled')
      btn.textContent = 'Create Account'
    } else {
      successMsg!.textContent = 'Account created! Check your email to confirm then sign in.'
      successMsg!.classList.remove('hidden')
      btn.textContent = 'Account Created'
    }
  })
</script>
