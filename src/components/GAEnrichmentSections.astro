---
import { R2_CDN } from '../lib/constants'

interface Props {
  enrichment: any
  r: any
  item: any
  currency?: string
}

const { enrichment, r, item, currency = 'GBP' } = Astro.props
const CURRENCY_SYMBOLS: Record<string, string> = { GBP: '£', USD: '$', NZD: 'NZ$', AUD: 'AU$', EUR: '€' }
const sym = CURRENCY_SYMBOLS[currency] || currency + ' '
const marketResearch = item.market_research_json || null
const mrEvidence: any[] = marketResearch?.evidence || []
const ebaySold: any[] = item.ebay_sold || []
---

<style is:global>
  /* ── EXPERT NOTES ── */
  .ga-expert-para {
    font-family: var(--font-ui);
    font-size: 15px;
    line-height: 1.9;
    color: var(--text-dim);
    margin-bottom: 14px;
  }
  .ga-expert-para:last-child { margin-bottom: 0; }

  /* ── TIMELINE ── */
  .ga-timeline { position: relative; padding-left: 22px; }
  .ga-timeline-line {
    position: absolute;
    left: 5px; top: 10px; bottom: 10px;
    width: 1px;
    background: var(--border);
  }
  .ga-tl-item {
    position: relative;
    padding: 10px 0 10px 14px;
    border-bottom: 1px solid var(--border);
  }
  .ga-tl-item:last-child { border-bottom: none; }
  .ga-tl-dot {
    position: absolute;
    left: -17px; top: 14px;
    width: 8px; height: 8px;
    border-radius: 50%;
    background: var(--surface-hi);
    border: 1.5px solid var(--border-hi);
    z-index: 1;
  }
  .ga-tl-dot.highlight { background: var(--ga-gold); border-color: var(--ga-gold); }
  .ga-tl-inner { display: flex; gap: 10px; align-items: baseline; }
  .ga-tl-year {
    font-family: var(--font-display);
    font-size: 16px;
    color: var(--text-mute);
    flex-shrink: 0;
    width: 48px;
  }
  .ga-tl-year.highlight { color: var(--ga-gold-hi); font-style: italic; }
  .ga-tl-label {
    font-family: var(--font-ui);
    font-size: 9px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--text-mute);
    margin-bottom: 2px;
  }
  .ga-tl-label.highlight { color: var(--ga-gold-hi); }
  .ga-tl-note {
    font-family: var(--font-ui);
    font-size: 14px;
    color: var(--text-dim);
    line-height: 1.5;
  }

  /* ── OPTIONS ── */
  .ga-options { display: flex; flex-direction: column; gap: 1px; background: var(--border); }
  .ga-option {
    padding: 16px;
    display: flex;
    align-items: flex-start;
    gap: 12px;
    background: var(--surface);
  }
  .ga-option.rec { background: var(--surface-hi); }
  .ga-option-dot {
    width: 6px; height: 6px;
    border-radius: 50%;
    flex-shrink: 0;
    margin-top: 5px;
    background: var(--border-hi);
  }
  .ga-option-dot.rec { background: var(--ga-gold); }
  .ga-option-action {
    font-family: var(--font-display);
    font-size: 16px;
    color: var(--text-dim);
    margin-bottom: 4px;
  }
  .ga-option-action.rec { color: var(--text); font-style: italic; }
  .ga-option-rec-badge {
    font-family: var(--font-ui);
    font-size: 9px;
    letter-spacing: 0.1em;
    color: var(--ga-gold-hi);
    margin-left: 10px;
  }
  .ga-option-detail {
    font-family: var(--font-ui);
    font-size: 14px;
    color: var(--text-mute);
    line-height: 1.5;
  }

  /* ── EBAY SOLD CARDS ── */
  .ga-sold-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-bottom: 8px;
  }
  @media (max-width: 480px) {
    .ga-sold-grid { grid-template-columns: 1fr; }
  }
  .ga-sold-card {
    background: var(--surface);
    border: 1px solid var(--border-hi);
    border-left: 3px solid var(--ga-gold);
    display: flex;
    flex-direction: column;
    overflow: hidden;
    text-decoration: none;
    color: inherit;
    transition: border-color 0.15s;
    position: relative;
  }
  .ga-sold-card:hover { border-color: var(--ga-gold-hi); }
  .ga-sold-thumb {
    height: 140px;
    background: var(--surface-hi);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    flex-shrink: 0;
  }
  .ga-sold-thumb img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    padding: 10px;
  }
  .ga-sold-thumb-placeholder {
    font-family: var(--font-display);
    font-size: 48px;
    color: var(--ga-gold);
    opacity: 0.2;
  }
  .ga-sold-body {
    padding: 12px 14px;
    display: flex;
    flex-direction: column;
    flex: 1;
  }
  .ga-sold-title {
    font-family: var(--font-ui);
    font-size: 13px;
    color: var(--text);
    line-height: 1.4;
    margin-bottom: 10px;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    flex: 1;
  }
  .ga-sold-price {
    font-family: var(--font-display);
    font-size: 20px;
    color: var(--ga-gold-hi);
    margin-bottom: 6px;
  }
  .ga-sold-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 8px;
  }
  .ga-sold-condition {
    font-family: var(--font-ui);
    font-size: 10px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--text-mute);
  }
  .ga-sold-date {
    font-family: var(--font-ui);
    font-size: 11px;
    color: var(--text-mute);
    flex-shrink: 0;
  }
  .ga-sold-format {
    font-family: var(--font-ui);
    font-size: 10px;
    color: var(--text-mute);
    margin-top: 4px;
  }
  .ga-sold-source {
    font-family: var(--font-ui);
    font-size: 10px;
    color: var(--text-mute);
    margin-top: 12px;
    padding-top: 12px;
    border-top: 1px solid var(--border);
    font-style: italic;
  }
  .ga-sold-card-del {
    position: absolute;
    top: 8px;
    right: 8px;
    width: 24px;
    height: 24px;
    background: rgba(0,0,0,0.5);
    border: none;
    color: var(--text-mute);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    transition: opacity 0.15s, color 0.15s;
    z-index: 2;
    font-size: 14px;
    line-height: 1;
  }
  .ga-sold-card:hover .ga-sold-card-del { opacity: 1; }
  .ga-sold-card-del:hover { color: #c46028; }
  .ga-sold-search-cta {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-top: 12px;
    padding: 12px 16px;
    background: var(--surface);
    border: 1px solid var(--border-hi);
    border-left: 3px solid var(--ga-gold);
    font-family: var(--font-ui);
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--ga-gold-hi);
    text-decoration: none;
    transition: border-color 0.15s, background 0.15s;
  }
  .ga-sold-search-cta:hover {
    background: var(--surface-hi);
    border-color: var(--ga-gold-hi);
  }

  /* ── MARKET RESEARCH ── */
  .ga-mr-summary {
    font-family: var(--font-ui);
    font-size: 15px;
    line-height: 1.8;
    color: var(--text-dim);
    margin-bottom: 20px;
  }
  .ga-mr-table {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 16px;
    font-family: var(--font-ui);
    font-size: 13px;
  }
  .ga-mr-table th {
    font-size: 9px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--text-mute);
    font-weight: 600;
    text-align: left;
    padding: 8px 12px;
    border-bottom: 1px solid var(--border-hi);
    background: var(--surface);
  }
  .ga-mr-table td {
    padding: 10px 12px;
    border-bottom: 1px solid var(--border);
    vertical-align: top;
    color: var(--text-dim);
    line-height: 1.4;
  }
  .ga-mr-table tr:last-child td { border-bottom: none; }
  .ga-mr-price {
    font-family: var(--font-display);
    font-size: 15px;
    color: var(--ga-gold-hi);
    font-style: italic;
    white-space: nowrap;
  }
  .ga-mr-badge {
    display: inline-block;
    font-family: var(--font-ui);
    font-size: 9px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-weight: 600;
    padding: 2px 7px;
    border-radius: 2px;
    white-space: nowrap;
  }
  .ga-mr-badge.verified { background: rgba(74,163,104,0.15); color: #4aa368; }
  .ga-mr-badge.estimate { background: rgba(var(--ga-gold-rgb,180,140,60),0.15); color: var(--ga-gold-hi); }
  .ga-mr-badge.dealer { background: rgba(100,120,200,0.15); color: #7a90d4; }
  .ga-mr-badge.inference { background: var(--surface-hi); color: var(--text-mute); }
  .ga-mr-source-link {
    font-size: 10px;
    color: var(--text-mute);
    text-decoration: none;
    letter-spacing: 0.05em;
  }
  .ga-mr-source-link:hover { color: var(--ga-gold-hi); }
  .ga-mr-venue {
    background: var(--surface);
    border: 1px solid var(--border-hi);
    border-left: 3px solid var(--ga-gold);
    padding: 14px 16px;
    margin-bottom: 12px;
  }
  .ga-mr-venue-label {
    font-family: var(--font-ui);
    font-size: 9px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    font-weight: 600;
    color: var(--ga-gold-hi);
    margin-bottom: 6px;
  }
  .ga-mr-venue-text {
    font-family: var(--font-ui);
    font-size: 14px;
    color: var(--text-dim);
    line-height: 1.6;
  }
  .ga-mr-limitation {
    font-family: var(--font-ui);
    font-size: 11px;
    color: var(--text-mute);
    font-style: italic;
    padding-top: 10px;
    border-top: 1px solid var(--border);
    line-height: 1.6;
  }
</style>

<script>
  import { supabase } from '../lib/supabase-browser'

  const { data: { session } } = await supabase.auth.getSession()

  document.querySelectorAll('[data-del-item]').forEach(btn => {
    btn.addEventListener('click', async (e) => {
      e.preventDefault()
      e.stopPropagation()
      const itemId = btn.dataset.delItem
      const identificationId = window.__val?.itemId
      if (!identificationId || !itemId || !session) return

      const { data: item } = await supabase
        .from('identifications')
        .select('ebay_sold')
        .eq('id', identificationId)
        .single()

      if (!item?.ebay_sold) return

      const updated = item.ebay_sold.filter(s => s.item_id !== itemId)

      const { error } = await supabase
        .from('identifications')
        .update({ ebay_sold: updated })
        .eq('id', identificationId)
        .eq('user_id', session.user.id)

      if (!error) {
        const card = document.querySelector(`[data-item-id="${itemId}"]`)
        card?.remove()
      }
    })
  })
</script>

<!-- ── EBAY SOLD CARDS — top of enrichment ── -->
{ebaySold.length > 0 && (
  <div class="ga-section">
    <div class="ga-section-head"><div class="ga-section-title">eBay Sold — Recent Sales</div></div>
    <div class="ga-sold-grid">
      {ebaySold.map((sale: any) => {
        const imageUrl = sale.r2_key ? `${R2_CDN}/${sale.r2_key}` : null
        const dateSold = sale.date_sold ? new Date(sale.date_sold).toLocaleDateString('en-GB', { day: 'numeric', month: 'short', year: 'numeric' }) : ''
        const totalPrice = sale.sale_price + (sale.shipping_price || 0)
        const priceDisplay = `${CURRENCY_SYMBOLS[currency] || currency + ' '}${totalPrice.toLocaleString('en', { minimumFractionDigits: 0, maximumFractionDigits: 2 })}`
        const bidsText = sale.bids ? ` · ${sale.bids} bids` : ''
        return (
          <div class="ga-sold-card" data-item-id={sale.item_id}>
            <button class="ga-sold-card-del" data-del-item={sale.item_id} title="Remove">&#x2715;</button>
            <a href={sale.item_link} target="_blank" rel="noopener noreferrer" style="display:contents;text-decoration:none;color:inherit;">
            <div class="ga-sold-thumb">
              {imageUrl ? (
                <img src={imageUrl} alt={sale.title} loading="lazy" />
              ) : (
                <div class="ga-sold-thumb-placeholder">{sym}</div>
              )}
            </div>
            <div class="ga-sold-body">
              <div class="ga-sold-title">{sale.title}</div>
              <div class="ga-sold-price">{priceDisplay}</div>
              <div class="ga-sold-meta">
                <span class="ga-sold-condition">{sale.condition_raw || sale.condition}</span>
                <span class="ga-sold-date">{dateSold}</span>
              </div>
              <div class="ga-sold-format">{sale.buying_format}{bidsText}</div>
            </div>
            </a>
          </div>
        )
      })}
    </div>
    <div class="ga-sold-source">
      Real sold prices from eBay. {ebaySold.length} listings returned for your search terms. Results may include reproduction, inspired or loosely related pieces alongside genuine examples. Use as market context. Click any card to view the original listing on eBay.
    </div>
    <a href={`/search/ebay?id=${item.id}`} class="ga-sold-search-cta">
      <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
      Search for more comparable sales
    </a>
  </div>
)}

<!-- ── MARKET VALUE CHART ── -->
{enrichment.price_history && enrichment.price_history.length > 0 && (() => {
  const prices = enrichment.price_history
  const values = prices.map((p: any) => p.value)
  const minVal = Math.min(...values)
  const maxVal = Math.max(...values)
  const chartW = 480
  const chartH = 160
  const padL = 48; const padR = 16; const padT = 12; const padB = 32
  const plotW = chartW - padL - padR
  const plotH = chartH - padT - padB
  const toX = (i: number) => padL + (i / (prices.length - 1)) * plotW
  const toY = (v: number) => padT + plotH - ((v - minVal) / (maxVal - minVal || 1)) * plotH
  const points = prices.map((p: any, i: number) => `${toX(i)},${toY(p.value)}`).join(' ')
  const areaPoints = `${padL},${padT + plotH} ${points} ${toX(prices.length-1)},${padT + plotH}`
  const peakY = enrichment.price_peak ? toY(enrichment.price_peak.value) : padT
  return (
    <div class="ga-section">
      <div class="ga-section-head"><div class="ga-section-title">Historical Market Context</div></div>
      {enrichment.price_context && <p class="ga-expert-para">{enrichment.price_context}</p>}
      <svg width="100%" viewBox={`0 0 ${chartW} ${chartH}`} style="display:block;overflow:visible">
        <defs>
          <linearGradient id="areaGrad" x1="0" y1="0" x2="0" y2="1">
            <stop offset="0%" stop-color="var(--ga-gold-hi)" stop-opacity="0.18"/>
            <stop offset="100%" stop-color="var(--ga-gold)" stop-opacity="0"/>
          </linearGradient>
        </defs>
        {enrichment.price_peak && <line x1={padL} y1={peakY} x2={chartW - padR} y2={peakY} stroke="var(--ga-gold)" stroke-width="1" stroke-dasharray="4 3" opacity="0.6"/>}
        <polygon points={areaPoints} fill="url(#areaGrad)"/>
        <polyline points={points} fill="none" stroke="var(--ga-gold-hi)" stroke-width="1.5" stroke-linejoin="round" stroke-linecap="round"/>
        {prices.map((p: any, i: number) => <circle cx={toX(i)} cy={toY(p.value)} r="3" fill="var(--ga-gold-hi)"/>)}
        {[minVal, Math.round((minVal+maxVal)/2), maxVal].map((v) => (
          <text x={padL - 6} y={toY(v) + 4} text-anchor="end" font-family="Inter, sans-serif" font-size="9" fill="var(--text-mute)">{sym}{v >= 1000 ? `${Math.round(v/1000)}k` : v}</text>
        ))}
        {prices.filter((_: any, i: number) => i % 2 === 0).map((p: any, i: number) => (
          <text x={toX(i * 2)} y={chartH - 4} text-anchor="middle" font-family="Inter, sans-serif" font-size="9" fill="var(--text-mute)">{p.year}</text>
        ))}
      </svg>
      {enrichment.price_peak && (
        <div style="display:flex;align-items:center;gap:8px;margin-top:8px">
          <div style="width:16px;border-top:1px dashed var(--ga-gold);"></div>
          <span style="font-family:var(--font-ui);font-size:9px;letter-spacing:0.1em;text-transform:uppercase;color:var(--text-mute)">Peak {sym}{enrichment.price_peak.value.toLocaleString()} ({enrichment.price_peak.year})</span>
        </div>
      )}
      <p style="font-family:var(--font-ui);font-size:10px;color:var(--text-mute);margin-top:12px;font-style:italic">AI-estimated historical trend. Not verified auction data. See eBay sold listings above for current market prices.</p>
    </div>
  )
})()}

<!-- ── MARKET RESEARCH ── -->
{marketResearch && (
  <div class="ga-section">
    <div class="ga-section-head"><div class="ga-section-title">Market Research</div></div>
    {marketResearch.summary && <p class="ga-mr-summary">{marketResearch.summary}</p>}

    {mrEvidence.length > 0 && (
      <table class="ga-mr-table">
        <thead>
          <tr>
            <th>Item</th>
            <th>Price</th>
            <th>Venue</th>
            <th>Date</th>
            <th>Evidence</th>
          </tr>
        </thead>
        <tbody>
          {mrEvidence.map((e: any) => {
            const badgeClass = e.evidence_type === 'verified_sale' ? 'verified'
              : e.evidence_type === 'catalogue_estimate' ? 'estimate'
              : e.evidence_type === 'dealer_asking' ? 'dealer'
              : 'inference'
            const badgeLabel = e.evidence_type === 'verified_sale' ? 'Verified Sale'
              : e.evidence_type === 'catalogue_estimate' ? 'Estimate'
              : e.evidence_type === 'dealer_asking' ? 'Dealer Asking'
              : 'Inference'
            return (
              <tr>
                <td>
                  {e.source_url
                    ? <a href={e.source_url} target="_blank" rel="noopener noreferrer" class="ga-mr-source-link">{e.description}</a>
                    : e.description
                  }
                </td>
                <td class="ga-mr-price">{e.price}</td>
                <td style="color:var(--text-mute)">{e.venue}</td>
                <td style="color:var(--text-mute);white-space:nowrap">{e.date}</td>
                <td><span class={`ga-mr-badge ${badgeClass}`}>{badgeLabel}</span></td>
              </tr>
            )
          })}
        </tbody>
      </table>
    )}

    {marketResearch.venue_recommendation && (
      <div class="ga-mr-venue">
        <div class="ga-mr-venue-label">Recommended Selling Venue</div>
        <div class="ga-mr-venue-text">{marketResearch.venue_recommendation}</div>
      </div>
    )}

    {marketResearch.data_limitation && (
      <div class="ga-mr-limitation">
        {marketResearch.data_limitation}
      </div>
    )}
  </div>
)}

<!-- ── DESIRABILITY INDEX ── -->
{enrichment.desirability_index && enrichment.desirability_index.length > 0 && (
  <div class="ga-section">
    <div class="ga-section-head"><div class="ga-section-title">Desirability Index</div></div>
    {enrichment.desirability_context && <p class="ga-expert-para">{enrichment.desirability_context}</p>}
    <div style="display:flex;flex-direction:column;gap:14px;">
      {enrichment.desirability_index.sort((a: any, b: any) => b.score - a.score).map((di: any) => (
        <div>
          <div style="display:flex;justify-content:space-between;align-items:baseline;margin-bottom:6px;">
            <span style={`font-family:var(--font-ui);font-size:10px;letter-spacing:0.1em;text-transform:uppercase;color:${di.this_piece ? 'var(--ga-gold-hi)' : 'var(--text-mute)'};`}>{di.name}{di.this_piece ? ' ← This piece' : ''}</span>
            <span style={`font-family:var(--font-display);font-size:14px;color:${di.this_piece ? 'var(--ga-gold-hi)' : 'var(--text-mute)'};`}>{di.score}</span>
          </div>
          <div style="height:3px;background:var(--border-hi);border-radius:2px;overflow:hidden;">
            <div style={`width:${di.score}%;height:100%;background:${di.this_piece ? 'linear-gradient(90deg,var(--ga-gold-hi),var(--ga-gold))' : 'var(--border)'};border-radius:2px;`}></div>
          </div>
        </div>
      ))}
    </div>
  </div>
)}

<!-- ── MAKER ── -->
{(enrichment.maker_detail || enrichment.manufacturer_detail) && (
  <div class="ga-section">
    <div class="ga-section-head"><div class="ga-section-title">{r.maker || 'Maker'}</div></div>
    {enrichment.maker_detail?.bio && <p class="ga-expert-para">{enrichment.maker_detail.bio}</p>}
    {enrichment.maker_detail?.notable_works && <p class="ga-expert-para">{enrichment.maker_detail.notable_works}</p>}
    {enrichment.manufacturer_detail?.history && (
      <>
        <div class="ga-section-head" style="margin-top:16px;"><div class="ga-section-title">Manufacturer</div></div>
        <p class="ga-expert-para">{enrichment.manufacturer_detail.history}</p>
        {enrichment.manufacturer_detail.relationship_to_maker && <p class="ga-expert-para">{enrichment.manufacturer_detail.relationship_to_maker}</p>}
      </>
    )}
  </div>
)}

<!-- ── MAKER MARK ── -->
{enrichment.maker_mark_explanation && (
  <div class="ga-section">
    <div class="ga-section-head"><div class="ga-section-title">Maker Mark</div></div>
    {r.category_specific?.maker_mark && (
      <div style="background:var(--surface);padding:12px 16px;margin-bottom:16px;border-left:3px solid var(--ga-gold);">
        <div style="font-family:var(--font-ui);font-size:10px;letter-spacing:0.1em;text-transform:uppercase;color:var(--text-mute);margin-bottom:6px;">Visible Mark</div>
        <div style="font-family:var(--font-ui);font-size:15px;color:var(--text);">{r.category_specific.maker_mark}</div>
      </div>
    )}
    <p class="ga-expert-para">{enrichment.maker_mark_explanation}</p>
  </div>
)}

<!-- ── TECHNICAL NOTES ── -->
{(enrichment.pattern_detail || enrichment.glaze_detail || enrichment.firing_detail) && (
  <div class="ga-section">
    <div class="ga-section-head"><div class="ga-section-title">Technical Notes</div></div>
    {r.category_specific?.pattern_name && <div style="font-family:var(--font-ui);font-size:10px;letter-spacing:0.1em;text-transform:uppercase;color:var(--text-mute);margin-bottom:8px;">Pattern — {r.category_specific.pattern_name}</div>}
    {enrichment.pattern_detail && <p class="ga-expert-para">{enrichment.pattern_detail}</p>}
    {r.category_specific?.glaze_type && <div style="font-family:var(--font-ui);font-size:10px;letter-spacing:0.1em;text-transform:uppercase;color:var(--text-mute);margin:16px 0 8px;">Glaze — {r.category_specific.glaze_type}</div>}
    {enrichment.glaze_detail && <p class="ga-expert-para">{enrichment.glaze_detail}</p>}
    {r.category_specific?.firing_type && <div style="font-family:var(--font-ui);font-size:10px;letter-spacing:0.1em;text-transform:uppercase;color:var(--text-mute);margin:16px 0 8px;">Firing — {r.category_specific.firing_type}</div>}
    {enrichment.firing_detail && <p class="ga-expert-para">{enrichment.firing_detail}</p>}
  </div>
)}

<!-- ── EXPERT NOTES ── -->
{enrichment.expert_notes && (
  <div class="ga-section">
    <div class="ga-section-head"><div class="ga-section-title">Provenance &amp; Expert Notes</div></div>
    {enrichment.expert_notes.map((para: string) => <p class="ga-expert-para">{para}</p>)}
    {enrichment.market_insight && <p class="ga-expert-para" style="margin-top:14px;padding-top:14px;border-top:1px solid var(--border)">{enrichment.market_insight}</p>}
  </div>
)}

<!-- ── TIMELINE ── -->
{enrichment.maker_timeline && enrichment.maker_timeline.length > 0 && (
  <div class="ga-section">
    <div class="ga-section-head"><div class="ga-section-title">{r.maker ? `${r.maker} · Timeline` : 'Timeline'}</div></div>
    <div class="ga-timeline">
      <div class="ga-timeline-line"></div>
      {enrichment.maker_timeline.map((entry: any) => (
        <div class="ga-tl-item">
          <div class={`ga-tl-dot${entry.highlight ? ' highlight' : ''}`}></div>
          <div class="ga-tl-inner">
            <div class={`ga-tl-year${entry.highlight ? ' highlight' : ''}`}>{entry.year}</div>
            <div>
              <div class={`ga-tl-label${entry.highlight ? ' highlight' : ''}`}>{entry.highlight ? 'This piece' : ''}</div>
              <div class="ga-tl-note">{entry.event}</div>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
)}

<!-- ── YOUR OPTIONS ── -->
{enrichment.your_options && enrichment.your_options.length > 0 && (
  <div class="ga-section">
    <div class="ga-section-head"><div class="ga-section-title">Your Options</div></div>
    <div class="ga-options">
      {enrichment.your_options.map((opt: any) => (
        <div class={`ga-option${opt.recommended ? ' rec' : ''}`}>
          <div class={`ga-option-dot${opt.recommended ? ' rec' : ''}`}></div>
          <div>
            <div class={`ga-option-action${opt.recommended ? ' rec' : ''}`}>{opt.action}{opt.recommended && <span class="ga-option-rec-badge">RECOMMENDED</span>}</div>
            <div class="ga-option-detail">{opt.detail}</div>
          </div>
        </div>
      ))}
    </div>
  </div>
)}
